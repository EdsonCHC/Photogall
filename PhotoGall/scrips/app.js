// Search animado
window.addEventListener("scroll", function () {//
    var search = document.querySelector(".form-container-r")
    search.classList.toggle("mostrar", window.scrollY > 350);
})
//Fin search animado

//espacio para mas código :v

//:C

